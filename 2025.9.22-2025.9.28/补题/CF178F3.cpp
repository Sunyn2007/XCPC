#include <bits/stdc++.h>
using namespace std;
const int N = 2000, L = 500, S = 4e5;
int n, k, trie[S][26], num, siz[S];
long long dp[S + 5][]
void ins(string s) {
    int now = 1, 
    for (int i = 0; i < s.size(); i++) {
        
    }
}
int main() {
    ios::sync_with_stdio(false);
    cin.tie(0), cout.tie(0);
    cin >> n >> k;
    for (int i = 1; i <= n; i++) {
        
    }
    return 0;
}