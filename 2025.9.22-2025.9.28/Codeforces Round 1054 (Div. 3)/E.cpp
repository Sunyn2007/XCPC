#include <bits/stdc++.h>
using namespace std;
const int N = 2e5;
int t, n, k, l, r, a[N + 5], f[N + 5];
void solve() {
    cin >> n >> k >> l >> r;
    
    return ;
}
int main() {
    ios::sync_with_stdio(false);
    cin.tie(0), cout.tie(0);
    cin >> t;
    while (t--) solve();
    return 0;
}