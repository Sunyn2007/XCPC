#include <bits/stdc++.h>
using namespace std;
int a, b, tot;
int main() {
    ios::sync_with_stdio(false);
    cin.tie(0), cout.tie(0);
    cin >> a >> b;
    tot = a * 10 + b;
    cout << tot / 19;
    return 0;
}