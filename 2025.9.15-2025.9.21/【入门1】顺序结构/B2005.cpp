#include <bits/stdc++.h>
using namespace std;
char ch;
int main() {
    ios::sync_with_stdio(false);
    cin.tie(0), cout.tie(0);
    cin >> ch;
    cout << "  " << ch << "  " << '\n';
    cout << " " << ch << ch << ch << " " << '\n';
    cout << ch << ch << ch << ch << ch;
    return 0;
}