#include <bits/stdc++.h>
using namespace std;
int a, b, c, ans;
int main() {
    ios::sync_with_stdio(false);
    cin.tie(0), cout.tie(0);
    cin >> a >> b >> c;
    ans = a * 0.2 + b * 0.3 + c * 0.5;
    cout << ans;
    return 0;
}